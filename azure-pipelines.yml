trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:

- task: Docker@2
  inputs:
    containerRegistry: 'dockerhub-connection'
    repository: 'aravindhjv/docker-demo'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: 'latest'